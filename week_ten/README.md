# Week Ten Dashboard App

A Flutter dashboard app built with Bloc state management.

Features:
- Login/logout flow with role selection
- Role-specific navigation for admin and user
- Dashboard cards and activity list screen
- Profile page and logout
- Repository layer for authentication and dashboard data

Run the app from `week_ten` with:

```bash
flutter pub get
flutter run
```
